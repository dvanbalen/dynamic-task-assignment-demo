import java.lang.Number;
import com.gnw.domain.Appraiser;
import com.gnw.domain.Property;

rule "FilterAppraisers"
	dialect "mvel"
	when
		$p : Property()
		$a : Appraiser(province == $p.province)
	then
		taskOwner = $a.first;
		System.out.println("Task assigned to "+taskOwner);
end
